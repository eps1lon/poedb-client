const initial = {
  data: null, // a description object
  name: null, // a string representation of the model
};

const model = (state = initial, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default model;
