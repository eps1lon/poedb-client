const initial = {
  data: [], // list of model names
  selected: -1, // key of #data
};

const model = (state = initial, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default model;
